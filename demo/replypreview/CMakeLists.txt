CMAKE_MINIMUM_REQUIRED (VERSION 3.16)

uise_demo(
    NAME replypreview-demo
    TITLE "Reply UI"
    DESCRIPTION "Reply preview block reused in the editor bar, the message bubble reply section and the full-preview dialog."
    SOURCES ${CMAKE_CURRENT_SOURCE_DIR}/main.cpp
)
