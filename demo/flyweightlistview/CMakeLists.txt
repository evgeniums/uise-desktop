CMAKE_MINIMUM_REQUIRED (VERSION 3.16)
PROJECT(flyweightlistview-demo)

SET (SOURCES
    ${CMAKE_CURRENT_SOURCE_DIR}/main.cpp
)

ADD_EXECUTABLE(${PROJECT_NAME} ${SOURCES})
TARGET_LINK_LIBRARIES(${PROJECT_NAME} ${CMAKE_PROJECT_NAME})
